site-layout
    @title -- PiggyBank | Results
    @body
        endpoint-app endpoints=endpoints

static {
    // Endpoints
    var endpoints = [ "info", "accounts", "balance", "transactions" ];
}

style {
    #content {
        height: 100vh;
        background-color: #eceeef;
    }
}
